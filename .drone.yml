pipeline:
  build:
    image: plugins/docker
    commands:
      - docker build -t nesatmo-plop .
  publish:        
    image: plugins/docker
    username: $docker_username
    password: $docker_password
    repo: nesousx/nesatmo-s6
    tags: latest
